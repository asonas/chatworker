<%= form_for(@icon, html: { multipart: true }) do |f| %>
  <% if @icon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@icon.errors.count, "error") %> prohibited this icon from being saved:</h2>

      <ul>
      <% @icon.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :class_name %> (ex. cw_aXXXXXXX)<br />
    <%= f.text_field :class_name %>
  </div>
  <div>
    <label>Icon file</label>10kbまで <br />
    <%= file_field_tag :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
